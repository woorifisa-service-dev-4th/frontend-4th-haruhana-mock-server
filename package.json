{
  "name": "haruhana-mock-server",
  "version": "1.0.0",
  "description": "Mock server for Haruhana statistics API",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --routes routes.json --port 8080 --host 0.0.0.0 --base /api"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
